{
  "name": "hl7.fhir.us.codex-ocpa",
  "version": "0.1.0",
  "private": true,
  "description": "SUSHI/IG Publisher project for the OCPA Implementation Guide",
  "scripts": {
    "sushi": "sushi .",
    "build": "PATH=$PWD/node_modules/.bin:$PATH ./_build.sh build",
    "build:once": "PATH=$PWD/node_modules/.bin:$PATH ./_genonce.sh",
    "build:watch": "PATH=$PWD/node_modules/.bin:$PATH ./_gencontinuous.sh",
    "publisher:update": "./_updatePublisher.sh"
  },
  "devDependencies": {
    "fsh-sushi": "3.19.0"
  }
}
